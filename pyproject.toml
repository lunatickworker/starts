[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[project]
name = "google-maps-calculator"
version = "1.0.0"
description = "Calculate reviews needed to reach Google Maps rating targets"
requires-python = ">=3.11,<3.12"
dependencies = [
    "streamlit==1.28.1",
    "playwright==1.45.0",
]

[tool.poetry]
name = "google-maps-calculator"
version = "1.0.0"
description = "Calculate reviews needed to reach Google Maps rating targets"
authors = ["Developer <dev@example.com>"]
python = "^3.11"

[tool.poetry.dependencies]
python = "^3.11"
streamlit = "1.28.1"
playwright = "1.45.0"
